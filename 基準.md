# 食器洗いサポート — 基準.md

最終更新: 2026-08-30
状態: 世界公開済み（Railway、独自ドメインpiscavi.com稼働・SSL有効）

---

## 概要

あきら本人へのヒアリング（第19課題）から生まれたアプリ。「食後の食器洗いが手間」という困りごとに対し、
洗った実績を記録してモチベーションを上げる（連続記録・ストリーク表示）MVPを実装。

---

## フォルダ構成

- `streamlit/app.py` — アプリ本体（記録・現在の連続記録・最長記録・記録日一覧）
- `streamlit/requirements.txt` — 依存パッケージ
- `streamlit/data/records.json` — 記録データ（.gitignore対象にする予定）
- `仕様書/仕様書.md` — ヒアリングメモ（誰の・どんな困りごと）
- `仕様書/MVP企画.md` — MVP機能の選定と「今回はやらないこと」

---

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `基準.md` | このプロジェクトの正本 |
| `streamlit/app.py` | アプリ本体 |
| `仕様書/MVP企画.md` | MVPの正本 |

---

## 禁止事項

- 重要ファイルの削除は必ず確認する
- 今後Claude API等のAPIキーを扱う際は環境変数管理とし、コード・リポジトリに直接書かない

---

## 現フェーズ

第19課題（ヒアリング）→MVP絞り込み→MVP実装→第20課題（外部API組み込み）→第21課題（Claude API組み込み）まで完了。
今日の記録に対してClaude（`encourage.py`）が短い応援コメントを生成する機能を実装（`ANTHROPIC_API_KEY`未設定時はNoneを返しアプリは落ちない設計、テスト済み）。
追加要望で「月単位の達成感ジグソーパズル」を実装済み。自前SVGイラスト（月替わりで正月/バレンタイン/ひな祭り/桜/鯉のぼり/紫陽花/七夕/花火/お月見/紅葉/クリスマス、`illustrations.py`）を7×5マスに分割し、記録した日のマスだけ本来の絵を表示、未記録はグレーアウト。CSS background-position計算をJSで検証済み（1日ごとに-60pxずつ正しくずれる）。全マス埋まると達成表示+バルーン演出。
Streamlit Community Cloudへのデプロイも完了し、世界公開URLが発行された。
8月の花火イラストは「線だけで花火と分からない」というフィードバックを受け、山下清の花火の絵を意識した、色とりどりの点(circle)を密に敷き詰めるちぎり絵風の表現に刷新（`_firework_burst`関数、5箇所のバーストで計2000個超のドット、シード固定で再現性あり）。
【不具合修正】Streamlit Community CloudのサーバーはUTCで動作するため、日本時間の日付とズレて「今日」の判定がおかしくなる不具合を発見・修正。`date.today()`を全廃し、JST(UTC+9)を明示的に指定する`today_jst()`関数に統一。

---

## Git×Railway上級マスター応用編（2026-08-30、第1〜15回 修了）

デプロイ基盤をStreamlit Community CloudからRailwayへ移行し、以下を整備済み。

- production・staging環境の`web`サービスに永続Volumeを接続し、`records.json`のデータ消失問題を解消（第14回、`仕様書/本番DB運用.md`）
- `schema_version`導入・原子的書き込み・破損検知・7世代バックアップ・staging限定の復元手順を整備
- GitHub Actions CI／Railway「Wait for CI」連携でCI/CDを完成（第13回、`仕様書/CI-CD流れ図.md`）
- 独自ドメイン`piscavi.com`取得・DNS/SSL・Healthcheck・UptimeRobot外部監視・障害復旧手順書（`仕様書/障害復旧手順書.md`）・開発〜運用の型（`仕様書/開発運用の型.md`）まで完了し、第15回「本番運用の総合演習」で正式修了（2026-08-30）
- 詳細な経緯は`E:\AIで作ったもの\クロードコード部\課題ログ.md`の「第14回」「第15回」セクションを参照

以降はSaaS本体編（第16回〜）。現在のJSON保存は複数人利用・マルチテナント化に不向きなため、第16回着手前にPostgreSQLへの移行を別作業として先行する方針（JSONバックアップ→移行→件数/内容照合→JSONへ戻せる状態を維持、を条件とする）。移行設計は`仕様書/PostgreSQL移行設計.md`を参照。

---

## 次のアクション

- [ ] PostgreSQL移行機能の実装（feature/postgres-migrationブランチで作業中。Railwayへの追加・本番データの変更はまだ行わない）
- [ ] UptimeRobotの監視先をRailway標準URLから独自ドメインへ切替え（任意対応）
- [ ] 実際にClaude APIコメントを見るには、`ANTHROPIC_API_KEY`をRailwayの環境変数（production／staging）に設定する必要あり

---

## 公開情報

リポジトリ: https://github.com/claude-code-club/shokki-arai-support （`streamlit/data/`は.gitignore対象、永続Volumeで管理）
本番URL（独自ドメイン、SSL有効）: https://www.piscavi.com/
本番URL（Railway標準、フォールバック）: https://web-production-0b8c5f.up.railway.app/
staging URL: https://web-staging-beab.up.railway.app/

---

## 作業ログタグ

`~/.claude/作業ログ/` で `[食器洗いサポート]` を検索
